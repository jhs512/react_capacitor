import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sbs.jhs.exam1',
  appName: 'exam1',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
